# SEO SPEC PART 2: META DESCRIPTIONS

- `/` → MCP firewall and DLP gateway for AI agents. Every tool call scanned for secrets, PII, prompt injection, and destructive commands. Free tier available.
- `/blog` → Technical writing on MCP security, AI agent threats, DLP for coding agents, and lessons from building mistaike.ai.
- `/blog/ai-agents-bugs` → Our AI agents logged 224 bugs in 4 days. Here's what the data shows about AI code generation patterns and how to catch them.
- `/docs` → API documentation for mistaike.ai. Integrate MCP firewall and DLP scanning into your AI agent workflow with Bearer token auth.
- `/docs/mcp-setup` → Connect Claude Code, Cursor, Gemini CLI, or GitHub Copilot to mistaike.ai in under 2 minutes. MCP hub setup with DLP included.
- `/pricing` → Free tier: 1,000 scanned requests per day. Pro and Enterprise plans for teams running MCP servers in production.
- `/about` → mistaike.ai is an MCP security gateway that scans every AI agent tool call for secrets, PII, and prompt injection. Built by Nick Stocks.
- `/mcp-firewall` → What is an MCP firewall? A security layer that scans every tool call between AI agents and MCP servers. mistaike.ai blocks secrets, PII, and attacks.
- `/mcp-gateway` → MCP security gateway that routes, scans, and controls every tool call. DLP, prompt injection blocking, and audit logging in one endpoint.
- `/sandboxed-mcp` → Upload your MCP server code. We auto-discover your APIs, wrap every call in DLP and content security, and host it in an isolated sandbox.
- `/memory-migration` → Migrate AI agent context and memory between different agents and platforms. Portable, secure, and auditable context transfer via MCP.
